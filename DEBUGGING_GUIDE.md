# دليل استكشاف أخطاء نظام رسوم التوصيل

## 🔍 كيفية تتبع الأخطاء

### الخطوة 1: فتح أدوات المتطور (Developer Tools)

**في المتصفح:**
- اضغط `F12` أو `Ctrl+Shift+I`
- اختر تبويب **Console**

**في السيرفر:**
- انظر إلى نافذة Terminal حيث تشغل `npm run dev`

---

## ❌ خطأ: "خطأ في حفظ الإعدادات" عند الحفظ

### الخطوات للتشخيص:

#### 1️⃣ افحص Console في المتصفح (F12)
ابحث عن:
```
📤 جاري إرسال الإعدادات: {...}
✅ البيانات المحضرة للإرسال: {...}
📥 الرد من الخادم: 400 (أو 500)
❌ فشل الحفظ: {error: "...", message: "..."}
```

#### 2️⃣ افحص السجلات في السيرفر (Terminal)
ابحث عن:
```
📥 تم استقبال طلب حفظ إعدادات التوصيل: {...}
🔄 البيانات بعد التحويل: {...}
❌ خطأ في حفظ إعدادات رسوم التوصيل: ...
```

#### 3️⃣ تحقق من رسالة الخطأ المحددة

---

## 🔴 الأخطاء الشائعة وحلولها

### الخطأ 1: "الحد الأقصى يجب أن يكون أكبر من الحد الأدنى"

**الرسائل في Console:**
```
❌ الحد الأقصى (20) يجب أن يكون أكبر من أو يساوي الحد الأدنى (50)
📊 التحقق من الحدود: minFee=50, maxFee=20
```

**الحل:**
1. ادخل الإعدادات مرة أخرى
2. تأكد من أن **الحد الأقصى > الحد الأدنى**
   - مثال خاطئ: minFee=50, maxFee=20 ❌
   - مثال صحيح: minFee=20, maxFee=50 ✅
3. اضغط الحفظ

---

### الخطأ 2: "ليست قيمة رقمية صحيحة"

**الرسائل في Console:**
```
❌ الرسوم الأساسية = "abc" ليست قيمة رقمية صحيحة
❌ رسوم لكل كيلومتر = "xyz" ليست قيمة رقمية صحيحة
```

**الحل:**
1. تأكد من أن جميع الحقول تحتوي على **أرقام فقط**
2. لا تدخل أحرف أو رموز
3. أمثلة صحيحة:
   - الرسوم الأساسية: `5` ✅
   - رسوم لكل كم: `2.5` ✅
   - الحد الأدنى: `0` ✅
4. أمثلة خاطئة:
   - الرسوم الأساسية: `5 ريال` ❌
   - رسوم لكل كم: `2.5%` ❌
   - الحد الأدنى: `zero` ❌

---

### الخطأ 3: "تعذر الاتصال بقاعدة البيانات"

**الرسائل في Console:**
```
❌ عدم القدرة على الاتصال بقاعدة البيانات
خطأ في الاتصال: تعذر الاتصال بقاعدة البيانات
```

**الحل:**
1. تأكد من أن PostgreSQL يعمل
2. تأكد من صحة بيانات الاتصال في `.env`:
   ```
   DATABASE_URL=postgres://user:password@localhost:5432/database
   ```
3. جرب إعادة تشغيل السيرفر:
   ```bash
   npm run dev
   ```
4. جرب إعادة تشغيل قاعدة البيانات

---

### الخطأ 4: "خطأ في البيانات المدخلة"

**الرسائل في Console:**
```
❌ أخطاء Zod validation: [
  { field: "type", message: "Invalid enum value", code: "invalid_enum_value" }
]
```

**الحل:**
1. تأكد من أن نوع الحساب محدد بشكل صحيح
2. القيم المقبولة:
   - `fixed` - رسوم ثابتة ✅
   - `per_km` - حسب المسافة ✅
   - `zone_based` - حسب المناطق ✅
   - `restaurant_custom` - حسب إعدادات المطعم ✅
3. تأكد من عدم وجود مسافات إضافية

---

## 📊 خطوات التصحيح التفصيلية

### مثال: محاولة حفظ إعدادات

#### الخطوة 1: ملء النموذج
```
نوع الحساب: per_km
الرسوم الأساسية: 5
رسوم لكل كيلومتر: 2
الحد الأدنى: 3
الحد الأقصى: 50
حد التوصيل المجاني: 0
خط العرض: 24.7136
خط الطول: 46.6753
```

#### الخطوة 2: افتح Console (F12)
ستري رسائل مثل:
```
📤 جاري إرسال الإعدادات: {
  type: "per_km",
  baseFee: "5",
  perKmFee: "2",
  minFee: "3",
  maxFee: "50",
  freeDeliveryThreshold: "0",
  storeLat: "24.7136",
  storeLng: "46.6753"
}
```

#### الخطوة 3: انتظر الرد
```
✅ البيانات المحضرة للإرسال: {...}
📥 الرد من الخادم: 201
📋 البيانات المستقبلة: {
  success: true,
  message: "تم حفظ الإعدادات بنجاح",
  settings: {...}
}
🎉 تم الحفظ بنجاح!
```

#### الخطوة 4: تأكيد في Server Logs
في نافذة Terminal تشغل `npm run dev`:
```
📥 تم استقبال طلب حفظ إعدادات التوصيل: {...}
🔄 البيانات بعد التحويل: {...}
✅ البيانات تم التحقق منها بنجاح: {...}
✓ الرسوم الأساسية: 5 → 5
✓ رسوم لكل كيلومتر: 2 → 2
✓ الحد الأدنى: 3 → 3
✓ الحد الأقصى: 50 → 50
✓ حد التوصيل المجاني: 0 → 0
✓ خط العرض: 24.7136 → 24.7136
✓ خط الطول: 46.6753 → 46.6753
🧹 البيانات بعد التنظيف: {...}
📊 التحقق من الحدود: minFee=3, maxFee=50
✨ إنشاء إعدادات جديدة
✅ تم الإنشاء بنجاح: {...}
```

---

## 🛠️ أدوات Debugging

### 1. فتح Network Tab (F12)
- اختر تبويب **Network**
- اضغط الحفظ
- انقر على الطلب `POST /api/delivery-fees/settings`
- اعرض **Response** لرؤية الرد من الخادم

### 2. فتح Application Tab
- اختر تبويب **Application** (أو Storage)
- اختر **Local Storage**
- ابحث عن البيانات المحفوظة (إن وجدت)

### 3. فحص Database مباشرة
```bash
# تسجيل الدخول إلى PostgreSQL
psql -U your_user -d your_database -h localhost

# عرض الجداول
\dt

# عرض البيانات
SELECT * FROM delivery_fee_settings;
SELECT * FROM delivery_zones;
```

---

## 🔧 رموز الحالة (HTTP Status Codes)

| الكود | المعنى | التفسير |
|------|--------|---------|
| 201 | Created | تم إنشاء الإعدادات بنجاح ✅ |
| 200 | OK | تم تحديث الإعدادات بنجاح ✅ |
| 400 | Bad Request | بيانات غير صحيحة ❌ |
| 500 | Server Error | خطأ في السيرفر ❌ |

---

## 📝 ملخص الرسائل المهمة

### رسائل النجاح ✅
```
✅ تم الحفظ بنجاح!
✅ البيانات تم التحقق منها بنجاح
✅ تم التحديث بنجاح
✅ تم الإنشاء بنجاح
```

### رسائل التحذير ⚠️
```
⚠️ خط العرض فارغة - سيتم استخدام القيمة الافتراضية 0
⚠️ تحذير: الحد الأقصى يبدو مرتفعاً جداً
```

### رسائل الخطأ ❌
```
❌ الرسوم الأساسية ليست قيمة رقمية صحيحة
❌ الحد الأقصى يجب أن يكون أكبر من الحد الأدنى
❌ خطأ في حفظ إعدادات رسوم التوصيل
❌ عدم القدرة على الاتصال بقاعدة البيانات
```

---

## 🎯 خطوات الفحص السريعة

### قبل الحفظ:
- [ ] جميع الأرقام صحيحة
- [ ] الحد الأقصى > الحد الأدنى
- [ ] الموقع محدد (إن لزم الأمر)
- [ ] لا توجد أحرف في الحقول الرقمية

### أثناء الحفظ:
- [ ] افتح Console (F12)
- [ ] انقر الحفظ
- [ ] راقب الرسائل المطبوعة
- [ ] اعرض رسالة الخطأ الكاملة

### بعد الحفظ:
- [ ] تحقق من رسائل النجاح
- [ ] افتح السيرفر logs
- [ ] تأكد من عدم وجود أخطاء
- [ ] اختبر الحساب من العميل

---

## 🚀 نصائح للتطوير

### فعّل Debugging المتقدم
```bash
# تشغيل مع تسجيل مفصل
DEBUG=* npm run dev

# أو
NODE_ENV=development npm run dev
```

### استخدم Browser DevTools
```javascript
// في Console يمكن فحص الكائنات مباشرة:
// 1. افتح Network Tab
// 2. انقر على الطلب
// 3. اعرض القسم Response و Preview
```

### تسجيل البيانات يدوياً
في laoحة التحكم (Admin UI):
```javascript
// في أي نقطة في الكود
console.log('نقطة تفتيش:', variableName);
```

---

## 📞 طلب المساعدة

إذا لم تحل المشكلة:

1. **اجمع المعلومات:**
   - لقطة من رسالة الخطأ
   - محتوى Console التكامل
   - أسطر السيرفر Logs
   - البيانات التي حاولت إدخالها

2. **وصّف المشكلة:**
   - ما الذي حاولت؟
   - ما الذي حدث؟
   - ما المتوقع أن يحدث؟

3. **اتبع الخطوات:**
   - جرّب مع بيانات بسيطة
   - امسح localStorage: `localStorage.clear()`
   - أعد تحميل الصفحة
   - جرّب متصفح آخر

---

**آخر تحديث: 2026-02-20**
